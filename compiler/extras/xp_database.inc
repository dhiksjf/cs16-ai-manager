/*
* XP System - Database Management (Include File)
* Author: Generated by AI
* Date: 2024-02-29
* Description: This include file provides functions for managing player XP and level data in a vault file.
*/

#if defined _xp_database_included
 #endinput
#endif
#define _xp_database_included

// Include necessary headers
#include <amxmodx>
#include <nvault>

// Define database-related functions

/**
 * @brief Loads player XP and level data from the vault file.
 * @param id The player's ID.
 * @note This function retrieves the player's XP and level from the vault
 *       using a unique key based on the player's userid. If the data
 *       is not found, the player's XP and level will remain at their
 *       default values (0 and 1, respectively).
 */
native load_player_data(id);

/**
 * @brief Saves player XP and level data to the vault file.
 * @param id The player's ID.
 * @note This function saves the player's current XP and level to the
 *       vault file, using a unique key based on the player's userid. The
 *       nvault_write function is called to ensure that the data is
 *       written to disk.
 */
native save_player_data(id);

// Instructions:
// 1. Include this file in your main plugin file: #include "xp_database.inc"
// 2. Ensure that the nvault module is loaded in your server.
// 3. Define VAULT_FILE in xp_constants.inc to specify the vault file name.
// 4. The load_player_data and save_player_data functions are declared as natives.
//    Implementations MUST be provided in the main .sma file.
// 5. Error handling:  Check nvault_open return value.  Check userid for validity.
// 6. Debugging: Use debug_xp messages to track data loading and saving.
// 7. Remember to declare the g_vault variable in your main .sma file.
// 8. Ensure xp_constants.inc is included so that XP_DEBUG is available.
