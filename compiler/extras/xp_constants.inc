/*
* XP System - Constants
* Author: Generated by AI
* Date: 2024-02-29
* Description: Defines constants used throughout the XP system plugin.
*/

#if defined _xp_constants_included
 #endinput
#endif
#define _xp_constants_included

// Plugin Settings
#define VAULT_FILE "xp_data.vault" // File to store XP and level data.

// XP Reward Amounts
#define XP_KILL 10 // XP awarded for killing an enemy.
#define XP_SPAWN 1 // XP awarded for spawning (optional).

// Leveling System
#define BASE_XP_REQUIREMENT 100 // Base XP required to reach level 1.
#define LEVEL_MULTIPLIER 50      // Multiplier for XP required per level.

// Maximum level (optional, can be removed if no limit is desired)
#define MAX_LEVEL 100

// Error messages (for debugging)
#define XP_ERROR_INVALID_PLAYER "[XP System] Error: Invalid player ID."

// Debugging
#define XP_DEBUG 1 // Set to 1 to enable debug messages, 0 to disable.

// Configuration file path (optional, if using a config file)
#define XP_CONFIG_FILE "addons/amxmodx/configs/xp_system.cfg"

// Define colors for messages (using AMXX color codes)
#define XP_COLOR_GREEN "\x04"
#define XP_COLOR_DEFAULT "\x03"

// Function to display debug messages (only if XP_DEBUG is enabled)
#if XP_DEBUG
stock debug_xp(const format[], any:...) {
 new buffer[256]
 vformat(buffer, charsmax(buffer), format, 1)
 log_amx("[XP System Debug] %s", buffer)
}
#else
stock debug_xp(const format[], any:...) {}
#endif

// Instructions:
// 1. Include this file in your main plugin file: #include "xp_constants.inc"
// 2. Modify the constants to suit your desired XP system configuration.
// 3. Ensure VAULT_FILE is writeable by the server.
// 4. If using XP_CONFIG_FILE, create the configuration file and populate it with CVAR settings.
