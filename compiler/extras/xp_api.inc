/*
* XP System - API (Include File)
* Author: Generated by AI
* Date: 2024-02-29
* Description: This include file defines the API functions for the XP system.
*/

#if defined _xp_api_included
 #endinput
#endif
#define _xp_api_included

// Forward declarations of native functions
native give_player_xp(id, xp);
native get_player_xp(id);
native get_player_level(id);
native get_xp_required_for_next_level(level);

// Instructions:
// 1. Include this file in your main plugin file: #include "xp_api.inc"
// 2. Implement the native functions in your main plugin file.
// 3. Use these functions to interact with the XP system.

// Example Usage (in your main plugin file):
// #include <amxmodx>
// #include "xp_api.inc"  //Ensure xp_constants.inc is included before this file
// #include "xp_constants.inc"
//
// public plugin_init()
// {
//    register_plugin("XP System", "1.0", "Author");
//
//    // Example usage:
//    new id = 1; // Replace with a valid player ID
//    give_player_xp(id, XP_KILL); // Give the player XP for a kill
//    new xp = get_player_xp(id);   // Get the player's current XP
//    new level = get_player_level(id); // Get the player's current level
//    new xp_needed = get_xp_required_for_next_level(level); //Get XP required for the next level
//
//    // Print the information (example):
//    client_print(id, print_chat, "[XP] You have %d XP, you are level %d.", xp, level);
//    client_print(id, print_chat, "[XP] You need %d XP to reach the next level.", xp_needed);
// }

// Example Implementation (in your main plugin file):
// // Global Variables (declared in the main plugin file)
// new g_xp[MAX_PLAYERS + 1];
// new g_level[MAX_PLAYERS + 1];
//
// // Example of giving XP to a player
// public give_player_xp(id, xp)
// {
//  if (!is_user_connected(id))
//   return;
//
//  g_xp[id] += xp;
//
//  // Level up if enough XP
//  while (g_xp[id] >= get_xp_required_for_next_level(g_level[id]))
//  {
//   g_xp[id] -= get_xp_required_for_next_level(g_level[id]);
//   g_level[id]++;
//
//   client_print(id, print_chat, "[XP] You leveled up! You are now level %d.", g_level[id]);
//  }
//
//  // Save player data (optional: save on every XP gain)
//  // save_player_data(id);
// }
//
// // Example of getting player XP
// public get_player_xp(id)
// {
//  return g_xp[id];
// }
//
// // Example of getting player level
// public get_player_level(id)
// {
//  return g_level[id];
// }
//
// // Calculate XP required for the next level (example function)
// public get_xp_required_for_next_level(level)
// {
//  return BASE_XP_REQUIREMENT + (level * LEVEL_MULTIPLIER);
// }
